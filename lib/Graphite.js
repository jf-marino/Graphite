/**
 * Defines a namespace for Graphite where all other objects will rest.
 */
(function(global) {

	var Graphite = {
		core: {},
		models: {},
		views: {},
        engines: {},
        config: {}
	};

	global.Graphite = Graphite;

})(this);
